import { Text } from "react-native";

export const NotificationDetailsView = ({ route }: any) => {
    const { additionalData } = route.params;
    
    // Use additionalData as needed
    
    return (
      <Text>Testing</Text>
    );
  }